"""Конфігурація. Ключ береться з .env — у код нічого не зашивається."""

import os
import pathlib
from dotenv import load_dotenv

ROOT = pathlib.Path(__file__).parent
load_dotenv(ROOT / ".env")

API_KEY = os.getenv("ANTHROPIC_API_KEY")
if not API_KEY:
    raise SystemExit(
        "Не знайдено ANTHROPIC_API_KEY.\n"
        "  cp .env.example .env   і впишіть ключ у .env"
    )

# ── каскад моделей ────────────────────────────────────────────
# Дорога модель — тільки там, де потрібне міркування.
# Роутер, guardrail і judge роблять просту роботу — їм вистачає дешевої.
MODEL      = os.getenv("ANTHROPIC_MODEL", "claude-sonnet-4-6")       # цикл агента
MODEL_FAST = os.getenv("ANTHROPIC_MODEL_FAST", "claude-haiku-4-5-20251001")  # допоміжні виклики

MAX_TOKENS = int(os.getenv("MAX_TOKENS", 1200))
MAX_TURNS  = int(os.getenv("MAX_TURNS", 6))

# Жорсткий ліміт вартості одного прогону. Реальний happy-path прогін (3 виклики,
# ланцюжок з двох інструментів) коштує ~$0.02 — ліміт з запасом, але не нескінченний.
MAX_COST_USD = float(os.getenv("MAX_COST_USD", 0.05))

# ── пороги ескалації на людину ────────────────────────────────
ESCALATE_ON = {
    "guardrail_block":  True,   # відповідь не пройшла перевірку
    "turns_exhausted":  True,   # агент не вклався в MAX_TURNS
    "tool_error":       True,   # інструмент повернув помилку, яку агент не обійшов
    "user_asked":       True,   # клієнт прямо попросив людину
    "no_tool_used":     True,   # агент відповів, жодного разу не звернувшись до бекенду
    "budget_exceeded":  True,   # прогін перевищив MAX_COST_USD
}

DATA_DIR = ROOT / "data"
OUT_DIR  = ROOT / "out"
OUT_DIR.mkdir(exist_ok=True)

# Наскрізний запит курсу. Один на всі девʼять модулів.
USER_QUERY = (
    "Посилка EE123456789UA не прийшла вже два тижні. "
    "Я хочу повернути гроші за доставку."
)

BASE_PROMPT = (
    "Ти — асистент підтримки поштового оператора. Відповідай стисло, українською, "
    "простим текстом без Markdown-таблиць та емодзі — це чат підтримки.\n\n"
    "Кажи лише те, що підтверджено результатами інструментів. Не вигадуй конкретних "
    "кроків, документів чи процедур, якщо жоден інструмент їх не повернув. Якщо не "
    "знаєш точної інструкції для наступного кроку — прямо запропонуй клієнту звернутися "
    "до відділення особисто або з'єднати з оператором підтримки, замість того щоб "
    "вигадувати деталі.\n\n"
    "Якщо клієнт стверджує факт, якого немає в результатах інструментів (наприклад, "
    "назву компанії-перевізника, вагу посилки тощо) — не підтверджуй його версію і не "
    "пропонуй свою натомість. Чесно скажи, що такої інформації в системі немає, навіть "
    "якщо впевнений, що клієнт помиляється."
)
