"""Конфігурація. Ключ береться з .env — у код нічого не зашивається."""

import os
import pathlib
from dotenv import load_dotenv

ROOT = pathlib.Path(__file__).parent
load_dotenv(ROOT / ".env")

API_KEY = os.getenv("ANTHROPIC_API_KEY")
if not API_KEY:
    raise SystemExit(
        "Не знайдено ANTHROPIC_API_KEY.\n"
        "  cp .env.example .env   і впишіть ключ у .env"
    )

MODEL      = os.getenv("ANTHROPIC_MODEL", "claude-sonnet-4-6")            # основна відповідь
MODEL_FAST = os.getenv("ANTHROPIC_MODEL_FAST", "claude-haiku-4-5-20251001")  # grade, rewrite, agentic-крок
# bot.py тепер на agentic RAG (run_agentic) за замовчуванням — складене
# питання може дати 4-5 окремих search_kb + фінальну відповідь із
# кількома розділами (реальний приклад — README). 800/6 було розраховано
# на static RAG, тут замало: 800 токенів ризикує обрізати структуровану
# відповідь, 6 turns — рівно межа для 5 пошуків + 1 фінальний крок, без
# запасу на складніше питання.
# 1200 виявилось замало на реальному питанні («що треба, щоб відкрити
# бізнес») — модель впиралась у MAX_TOKENS і зупинялась на півслові
# (раніше це мовчки віддавалося як завершена відповідь, тепер
# core/agent.py позначає такий випадок outcome="truncated"). 1800 —
# запас навіть для широкого питання, разом із BASE_PROMPT, що тепер явно
# просить компактність (3-4 пункти замість вичерпного переліку), а не
# розрахунок на те, що ліміт узагалі не буде досягнуто.
MAX_TOKENS = int(os.getenv("MAX_TOKENS", 1800))
MAX_TURNS  = int(os.getenv("MAX_TURNS", 8))    # ліміт кроків для tool-use циклу (etap 2b)

# Потрібні лише для bot.py. Необов'язкові тут (щоб CLI-скрипти й далі
# працювали без них) — bot.py сам перевірить і впаде з ясним повідомленням.
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
# Кому слати сповіщення про нову редакцію закону (watch.py). Власний
# numeric chat_id — дізнатися можна написавши боту /start і подивившись
# у лог (bot.py друкує chat_id кожного вхідного повідомлення).
ADMIN_CHAT_ID = os.getenv("ADMIN_CHAT_ID")

# Персона агента. "Спирайся ЛИШЕ на витяг" — головна інструкція проти вигадування
# статей: без неї модель зі своїх довчасних знань підмішає щось правдоподібне,
# але не обов'язково точне чи чинне.
BASE_PROMPT = (
    "Ти — юридичний асистент-консультант із широкого кола питань права "
    "України (189 відслідковуваних кодексів і законів — від ПДР і "
    "Кримінального кодексу до трудового, сімейного, господарського, "
    "земельного права тощо). НІКОЛИ не вирішуй заздалегідь, що тема поза "
    "твоєю спеціалізацією, — базу знань визначає лише реальний результат "
    "пошуку, а не твоє припущення про неї. Відповідай стисло, українською, "
    "простим текстом без Markdown-таблиць.\n\n"
    "Це чат (Telegram), не юридичний документ. НЕ пиши заголовки ВЕЛИКИМИ "
    "ЛІТЕРАМИ, не став розділові лінії «---», не намагайся охопити геть усі "
    "аспекти теми — вибери 3-4 найважливіші пункти для конкретного питання "
    "замість вичерпного переліку. Короткі абзаци, за потреби — прості "
    "нумеровані пункти без вкладеної ієрархії. Якщо після цього все одно "
    "лишається багато деталей — краще запропонувати уточнити, що саме "
    "цікавить, ніж намагатися вмістити все одразу.\n\n"
    "Спирайся ЛИШЕ на витяг із бази знань, наведений нижче. Якщо там немає "
    "відповіді на питання — чесно скажи, що в базі цього немає, і порадь "
    "звернутися до фахівця чи звірити з актуальною редакцією закону. "
    "Ніколи не вигадуй номери статей чи пунктів.\n\n"
    "Кожен витяг із бази знань закінчується посиланням у дужках — це "
    "точне посилання на відповідний абзац на zakon.rada.gov.ua. "
    "Наприкінці відповіді додай розділ «Джерела:» зі списком посилань "
    "саме тих статей/пунктів, на які ти спирався (без дублів, без "
    "посилань на витяги, якими не користувався).\n\n"
    "Це навчальний прототип, а не офіційна юридична консультація."
)
